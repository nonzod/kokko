apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: badia-proxy-ingress
  namespace: http
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: badia-proxy-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required - badia"
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: "letsencrypt-office"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - badia.nicolatomassoni.it
      secretName: badia-proxy-tls
  rules:
  - host: "badia.nicolatomassoni.it"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: badia-proxy-service
            port:
              number: 1313
