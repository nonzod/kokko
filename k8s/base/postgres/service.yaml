apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: postgres
spec:
  ports:
  - port: 5432
  selector:
    app: postgres
---
# LoadBalancer per connessioni dirette
apiVersion: v1
kind: Service
metadata:  
  name: postgres-lb
  namespace: postgres
spec:
  type: LoadBalancer
  ports:
    - name: "5678"
      port: 5678
      targetPort: 5678
      protocol: TCP